<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 27+ specific theme attributes -->
    <style name="Theme.SmilePile.ChildFriendly" parent="Theme.Material3.Light">
        <!-- Navigation bar - API 27+ only -->
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!-- Full Screen Theme for Photo Viewer - API 27+ -->
    <style name="Theme.SmilePile.FullScreen" parent="Theme.SmilePile.ChildFriendly">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:navigationBarColor">@color/transparent</item>
    </style>
</resources>